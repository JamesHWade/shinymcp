# Build a typed HTML result

Build a typed HTML result

## Usage

``` r
mcp_result_html(html, model_value = NULL, text = NULL)
```

## Arguments

- html:

  HTML string or htmltools tag object.

- model_value:

  Optional machine-facing value.

- text:

  Optional plain-text fallback.
