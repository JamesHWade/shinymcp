# Build a typed text result

Build a typed text result

## Usage

``` r
mcp_result_text(value, model_value = value)
```

## Arguments

- value:

  Text value.

- model_value:

  Optional machine-facing value. Defaults to `value`.
