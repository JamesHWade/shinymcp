# Build a typed PDF result

Build a typed PDF result

## Usage

``` r
mcp_result_pdf(path_or_data, model_value = NULL, text = NULL)
```

## Arguments

- path_or_data:

  PDF path or raw/base64 data.

- model_value:

  Optional machine-facing value.

- text:

  Optional plain-text fallback.
