# Build a typed table result

Build a typed table result

## Usage

``` r
mcp_result_table(data, model_value = data, text = NULL)
```

## Arguments

- data:

  A data frame, matrix, or table-like object.

- model_value:

  Optional machine-facing value. Defaults to `data`.

- text:

  Optional plain-text fallback.
