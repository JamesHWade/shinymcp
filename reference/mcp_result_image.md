# Build a typed image result

Build a typed image result

## Usage

``` r
mcp_result_image(path_or_data, model_value = NULL, text = NULL)
```

## Arguments

- path_or_data:

  Image path or raw/base64 data.

- model_value:

  Optional machine-facing value.

- text:

  Optional plain-text fallback.
